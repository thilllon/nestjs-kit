---
"@nestjs-kit/redis": major
---

Add a client-agnostic Redis module: register `connect` and `disconnect` functions for any client library, inject the native client per alias, and close it on application shutdown.
